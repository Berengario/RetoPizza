@model IEnumerable<ProyectoPizzas.Models.Pizza>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <!--Html.DisplayNameFor(model => model.Izena)-->
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <!--aquí una arroba-->Html.DisplayFor(modelItem => item.Izena)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="<!--aquí una arroba-->item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="<!--aquí una arroba-->item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="<!--aquí una arroba-->item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
